import { switchSidebar } from './UI';

const allActions = {
  switchSidebar
};

export default allActions;

//return (dispatch: Dispatch) => {} *IMPORT FROM REDUX*
//axios.get<INTERFACE>(url)
//dispatch<INTERFACE>. INTERFACE should describe the type and payload.
